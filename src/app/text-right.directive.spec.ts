import { TextRightDirective } from './text-right.directive';

describe('TextRightDirective', () => {
  it('should create an instance', () => {
    const directive = new TextRightDirective();
    expect(directive).toBeTruthy();
  });
});
